version: '3'
services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: users_db
      POSTGRES_PASSWORD: users_db
      POSTGRES_DB: users_db
  app:
    build: .
    command: npm run start:dev
    restart: always
    ports:
      - "3000:3000"
    depends_on:
      - db
volumes:
  db_data: